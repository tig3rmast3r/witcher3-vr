# External dependencies

This directory is populated locally by `scripts/setup-dependencies.ps1`.
Dependency contents are intentionally excluded from Git. The script checks out
the exact pinned revisions used by the project.

Dependency licenses remain upstream property. The public attribution,
redistribution notices, and pinned revisions are recorded in
`../THIRD_PARTY_NOTICES.md`.
